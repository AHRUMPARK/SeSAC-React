// @ts-check

const arr = [1,2,3,4,5];
console.log(arr);

const resultArr = arr.map((el, index) => el * 3 );
  //모든 배열 
  // console.log(el);
  //몇번째 index인지
  // console.log(index);  
//  return el * 3;

console.log(resultArr);